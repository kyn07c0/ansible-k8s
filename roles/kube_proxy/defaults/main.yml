---

kube_proxy_version: 1.33.0

kube_proxy_extract_dir: /opt
kube_proxy_binary_dir: /usr/local/bin
kube_proxy_config_dir: /etc/kubernetes

kube_proxy_install_mode: network
kube_proxy_local_binary_dir: /home/yura/k8s/binary

kube_proxy_kubeconfig_dir: /etc/kubernetes
